dependencies {
	compileOnly project(":modules:streaming-dashboard:streaming-dashboard-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../streaming-service-builder-api/src/main/java"
}

group = "com.streaming"